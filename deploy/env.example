# Copy to /etc/forge-agent/env, chmod 600, chown root:root
ANTHROPIC_API_KEY=sk-ant-...
# Optional: dead-man's switch. Absent = monitoring disabled (no-op).
HEALTHCHECK_URL=https://hc-ping.com/your-uuid-here
